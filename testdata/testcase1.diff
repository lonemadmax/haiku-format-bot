From df4a018649f35d03ae7d197bcad701363546150b Mon Sep 17 00:00:00 2001
From: Niels Sascha Reedijk <niels.reedijk@gmail.com>
Date: Mon, 25 Dec 2023 21:34:58 +0000
Subject: [PATCH] Test commit for the format-checker

* Clock.rdef - change in file that is not going to be tested by clang-format
* cl_wind.cpp - two changes that will need to be reformated
* cl_view.cpp - rename a variable, file should already be in the correct format
* clock.h - no line changes (mode change only)

Change-Id: Ib68363538b702b8ce34dd3f48ea70a552d9e0afa
---
 src/apps/clock/Clock.rdef  |  2 +-
 src/apps/clock/cl_view.cpp | 20 ++++++++++----------
 src/apps/clock/cl_wind.cpp | 38 +++++++++++++++++++-------------------
 src/apps/clock/clock.h     |  0
 4 files changed, 30 insertions(+), 30 deletions(-)
 mode change 100644 => 100755 src/apps/clock/clock.h

diff --git a/src/apps/clock/Clock.rdef b/src/apps/clock/Clock.rdef
index 91d3a77f60..8b19c4047a 100644
--- a/src/apps/clock/Clock.rdef
+++ b/src/apps/clock/Clock.rdef
@@ -5,7 +5,7 @@ resource app_name_catalog_entry "x-vnd.Haiku-Clock:System name:Clock";
 
 resource app_version {
 	major  = 1,
-	middle = 0,
+	middle = 1,
 	minor  = 0,
 
 	variety = B_APPV_FINAL,
diff --git a/src/apps/clock/cl_view.cpp b/src/apps/clock/cl_view.cpp
index 43c17ee28d..4829b462e2 100644
--- a/src/apps/clock/cl_view.cpp
+++ b/src/apps/clock/cl_view.cpp
@@ -46,28 +46,28 @@ TOffscreenView::TOffscreenView(BRect frame, const char* name, short mRadius, sho
 dummy_label:
 	if (error == B_OK) {
 		size_t len;
-		void* picH;
+		void* picHelp;
 		BRect theRect(0, 0, 82, 82);
 		for (short loop = 0; loop <= 8; loop++) {
-			if ((picH = rsrcs.FindResource('PICT', loop + 4, &len))) {
+			if ((picHelp = rsrcs.FindResource('PICT', loop + 4, &len))) {
 				fClockFace[loop] = new BBitmap(theRect, B_CMAP8);
-				fClockFace[loop]->SetBits(picH, len, 0, B_CMAP8);
-				free(picH);
+				fClockFace[loop]->SetBits(picHelp, len, 0, B_CMAP8);
+				free(picHelp);
 			}
 		}
 
 		theRect.Set(0, 0, 15, 15);
-		if ((picH = rsrcs.FindResource(B_MINI_ICON_TYPE, "center", &len))) {
+		if ((picHelp = rsrcs.FindResource(B_MINI_ICON_TYPE, "center", &len))) {
 			fCenter = new BBitmap(theRect, B_CMAP8);
-			fCenter->SetBits(picH, len, 0, B_CMAP8);
-			free(picH);
+			fCenter->SetBits(picHelp, len, 0, B_CMAP8);
+			free(picHelp);
 		}
 
 		theRect.Set(0, 0, 2, 2);
-		if ((picH = rsrcs.FindResource('PICT', 13, &len))) {
+		if ((picHelp = rsrcs.FindResource('PICT', 13, &len))) {
 			fInner = new BBitmap(theRect, B_CMAP8);
-			fInner->SetBits(picH, len, 0, B_CMAP8);
-			free(picH);
+			fInner->SetBits(picHelp, len, 0, B_CMAP8);
+			free(picHelp);
 		}
 	}
 
diff --git a/src/apps/clock/cl_wind.cpp b/src/apps/clock/cl_wind.cpp
index 6aa6b5f398..060bccef9c 100644
--- a/src/apps/clock/cl_wind.cpp
+++ b/src/apps/clock/cl_wind.cpp
@@ -22,7 +22,7 @@ TClockWindow::TClockWindow(BRect frame, const char* title)
 		B_NOT_RESIZABLE | B_NOT_ZOOMABLE | B_AVOID_FRONT, B_ALL_WORKSPACES),
 	  fOnScreenView(NULL)
 {
-	_InitWindow();
+		_InitWindow();
 }
 
 
@@ -34,22 +34,22 @@ TClockWindow::~TClockWindow()
 bool
 TClockWindow::QuitRequested()
 {
-	BPath path;
-	if (find_directory (B_USER_SETTINGS_DIRECTORY, &path, true) == B_OK) {
-		path.Append("Clock_settings");
-		int ref = creat(path.Path(), 0777);
-		if (ref >= 0) {
-			BPoint lefttop = Frame().LeftTop();
-			write(ref, (char *)&lefttop, sizeof(BPoint));
-			short face = fOnScreenView->ReturnFace();
-			write(ref, (char *)&face, sizeof(short));
-			bool seconds = fOnScreenView->ReturnSeconds();
-			write(ref, (char *)&seconds, sizeof(bool));
-			close(ref);
+		BPath path;
+		if (find_directory (B_USER_SETTINGS_DIRECTORY, &path, true) == B_OK) {
+			path.Append("Clock_settings");
+			int ref = creat(path.Path(), 0777);
+			if (ref >= 0) {
+				BPoint lefttop = Frame().LeftTop();
+				write(ref, (char *)&lefttop, sizeof(BPoint));
+				short face = fOnScreenView->ReturnFace();
+				write(ref, (char *)&face, sizeof(short));
+				bool seconds = fOnScreenView->ReturnSeconds();
+				write(ref, (char *)&seconds, sizeof(bool));
+				close(ref);
+			}
 		}
-	}
-	be_app->PostMessage(B_QUIT_REQUESTED);
-	return true;
+		be_app->PostMessage(B_QUIT_REQUESTED);
+		return true;
 }
 
 
@@ -58,7 +58,7 @@ TClockWindow::_InitWindow()
 {
 	// half second pulse rate
 	SetPulseRate(500000);
-	
+
 	fOnScreenView = new TOnscreenView(BRect(0, 0, 82, 82), "Clock", 22, 15, 41);
 	AddChild(fOnScreenView);
 
@@ -80,9 +80,9 @@ TClockWindow::_InitWindow()
 			fOnScreenView->ShowSecs(secs);
 
 			close(ref);
-			
+
 			MoveTo(leftTop);
- 
+
 			BRect frame = Frame();
 			frame.InsetBy(-4, -4);
 			// it's not visible so reposition. I'm not going to get
diff --git a/src/apps/clock/clock.h b/src/apps/clock/clock.h
old mode 100644
new mode 100755
-- 
2.39.2

